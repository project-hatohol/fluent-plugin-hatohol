<source>
  type forward
</source>

<match hatohol.**>
  type hatohol

  # buffer_type file
  # buffer_path /tmp/fluentd/buffer
  flush_interval 1

  host localhost
  queue_name "hatohol.gate.#{ENV['HATOHOL_SERVER_ID'] || '1'}"
</match>
